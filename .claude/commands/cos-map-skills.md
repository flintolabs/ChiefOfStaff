Run a skill map showing how the user's existing expertise connects to a specific vision.

The vision to map against is: $ARGUMENTS
If no vision was specified, ask: "Which vision are we mapping your skills for?"

Run @.claude/scripts/read-context.md to load expertise and vision files. Then execute the skill mapping behavior from @.claude/skills/skill-mapping/skill-mapping.md.
