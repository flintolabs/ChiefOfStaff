Define 2–3 concrete, observable metrics for a specific vision.

The vision to define metrics for is: $ARGUMENTS
If no vision was specified, ask: "Which vision are we defining metrics for?"

Run @.claude/scripts/read-context.md to load vision files. Then execute the metrics behavior from @.claude/skills/metrics/metrics.md.
