Map the relationship types needed to advance a specific vision.

The vision to map network for is: $ARGUMENTS
If no vision was specified, ask: "Which vision are we mapping the network for?"

Run @.claude/scripts/read-context.md to load expertise and vision files. Then execute the network mapping behavior from @.claude/skills/network-mapping/network-mapping.md.
